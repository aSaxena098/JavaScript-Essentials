const obj = {
    firstName: 'Jonas',
    lastName: 'Saxena',
    totalFriends: ['Miachael','Anubhav','Sehar']
};
console.log(`${obj['firstName']} has ${obj['totalFriends'].length} friends but ${obj['totalFriends'][0]} is his best friend.!!`);
console.log(`${obj.firstName} has ${obj.totalFriends.length} friends but ${obj.totalFriends[0]} is his best friend.`);
